import { HighlightDayDirective } from './highlight-day.directive';

describe('HighlightDayDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightDayDirective();
    expect(directive).toBeTruthy();
  });
});
